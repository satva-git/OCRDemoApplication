@model OCRDemoApplication.Models.ApiResult
@{
    ViewBag.Title = "Data for " + @Request.QueryString["fileName"];
}
<h1>@ViewBag.Title</h1>
<h1>Income Statement</h1>
@if (@Model.DataDictionary1 != null)
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th scope="col">Key</th>
                <th scope="col">Value</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var dict in Model.DataDictionary1)
            {
                <tr>
                    @foreach (var key in dict.Keys)
                    {
                        <td>@dict[key]</td>
                    }
                </tr>
            }
        </tbody>
    </table>
}
